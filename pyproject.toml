[project]
name = "forest-carbon-3d-perception"
version = "0.1.0"
description = "3D perception + activity recognition pipeline for forest carbon/biodiversity MRV"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pycolmap>=3.10",
    "mlflow>=2.16",
    "torch>=2.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
